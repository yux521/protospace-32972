<main class="main">
  <div class="inner">
      <% if user_signed_in? %>
    <%# ログインしているときは以下を表示する %>
      <div class="greeting"> 
        こんにちは、
        <%= link_to "#{current_user.name}さん", user_path(current_user.id), class: :greeting__link %>
      </div>
    <% end %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      <%= render partial: "prototype" , collection: @prototypes %>
  <%# _prototype.html.erbを部分テンプレートとして、要素分繰り返し読み込む ※eachメソッドで繰り返し一覧表示していたものを、collectionオプションを使うことで、一行で記述できます。 %>
    </div>
  </div>
</main>
